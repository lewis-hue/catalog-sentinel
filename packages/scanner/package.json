{
  "name": "@sentinel/scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Distributor deep-scan: DistributorScanner interface, DistroKid adapter (resilient locators), canonical catalog normalization, and issue detection.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@sentinel/core": "*",
    "@sentinel/matching": "*",
    "playwright": "^1.49.1"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
